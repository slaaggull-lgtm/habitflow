services:
  - type: web
    name: habitflow
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
    disk:
      name: habitflow-data
      mountPath: /app
      sizeGB: 1
